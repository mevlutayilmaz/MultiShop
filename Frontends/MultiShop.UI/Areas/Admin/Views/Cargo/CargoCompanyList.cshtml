@model List<ResultCargoCompanyDTO>

@{
    ViewData["Title"] = "CargoCompanyList";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 1;
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <h4>Cargo Company List</h4>
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Company Name</th>
                                <th>Delete</th>
                                <th>Update</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@count</th>
                                    <td>@item.Name</td>
                                    <td><a href="/Admin/Cargo/DeleteCargoCompany/@item.Id" class="btn btn-danger">Delete</a></td>
                                    <td><a href="/Admin/Cargo/UpdateCargoCompany/@item.Id" class="btn btn-success">Update</a></td>
                                </tr>
                                count++;
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/Cargo/CreateCargoCompany" class="btn btn-primary">Add Cargo Company</a>
                </div>
            </div>
        </div>
    </div>
</section>


